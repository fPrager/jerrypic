# Port the Express server listens on (Caddy proxies the subdomain to this port).
PORT=3000

# Directory where uploaded images are stored (mounted as a Docker volume in prod).
DATA_DIR=/data

# Max accepted upload size in bytes (25 MB).
MAX_BYTES=26214400
